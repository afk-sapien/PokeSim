# Storage confirmation recovery

Hourly monitoring found Red Sprout repeating a storage release prompt at Indigo
Plateau. Frames and autosaves continued, but useful progress had stopped for over
30 minutes. The other two adventures continued training and League runs.

The Yes/No classifier inspected text behind the menu. HITMONLEE on a storage row
caused it to reject the confirmation as ordinary dialogue. The classifier now
reads complete labels in the selected menu column. Existing policy checks still
verify the selected individual and its protections before confirming release.

The same private checkpoint made no storage progress over 30,000 baseline frames.
The corrected replay released one selected spare Mr. Mime in 474 frames. Its
remaining counterpart, the party, the Pokédex, and all other stored records were
preserved. Regression validation passed 954 tests with 40 skipped.

The candidate wheel was derived from the exact installed wheel, changing only
`pokesim/screen.py` and its package checksum record. Candidate SHA256:
`866f056504c1e2496f389a80ec3e92a7d122a06563474146ddefeacb74a08ec5`.
Private replay evidence and the deployed wheel are under the local smoke data
directory in `observations/storage-confirmation-20260918`. The complete cold
backup and previous wheel are at
`/home/ty/.local/share/pokesim/backups/storage-confirmation-20260918`.

All three latest copied saves loaded before installation. After restart every
campaign retained its identity and Pokédex count, with advancing frames, zero
recovery reloads, and no trade holds. Sprout freed a slot and defeated Agatha,
then continued toward Lance. A short recovery check does not establish
long-term endurance.
