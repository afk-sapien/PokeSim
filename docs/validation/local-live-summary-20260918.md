# Compact Live summary

Replaced the slogan, standalone clock and trainer card with one compact header.
All trainer details remain visible. The simulation, plan and party begin roughly
100 pixels higher on desktop. Desktop header height is 70.5 pixels. Phone previews
at 390 and 320 pixels have no horizontal overflow. All eight runtime JavaScript
test files passed. Both source checkouts contain the changes.

Published index.html and live.css without a restart. Readiness reports all three
adventures healthy. Backup files and current checksums are recorded in:
`/home/ty/.local/share/pokesim/backups/live-simplify-20260918/deployment.json`

The existing hourly monitor reads these updated checksums.
